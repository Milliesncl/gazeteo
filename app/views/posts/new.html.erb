  <%= simple_form_for [@post] do |f| %>
    <div class="form-group">
      <%= f.input :description, label: "Description" %>
      <%= f.input :photo, as: :file, label: "Télécharger une photo" %>
    </div>
    <div class="task-btn">
      <%= f.submit "Ajouter une publication" %>
    </div>
  <% end %>
