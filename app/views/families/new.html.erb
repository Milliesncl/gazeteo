<h1>Nouvelle famille</h1>

<%= form_with(model: @family) do |f| %>
  <%= f.label(:name, "Nom de la famille :") %>
  <%= f.text_field(:name) %>

  <%= f.file_field :photo %>
  <%= f.submit "Create" %>
<% end %>
