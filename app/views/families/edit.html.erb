<h1>Editer ma famille</h1>
<div class="card edit-family">
  <h2>Ajouter des utilisateurs</h2>
  <%= form_with(url: user_invitation_path, method: "patch") do %>
    <%= label_tag(:email, "Inviter:") %>
    <%= email_field(:user, :email) %>
    <%= submit_tag("Ajouter", class: "btn-ghost") %>
  <% end %>

  <h2> Modifier la photo de famille </h2>
  <%= form_with(model: @family) do |f| %>
    <%= f.file_field :photo, class: "custom-file-input" %>
    <%= f.submit "Modifier", class: "btn-ghost" %>
  <% end %>
</div>
